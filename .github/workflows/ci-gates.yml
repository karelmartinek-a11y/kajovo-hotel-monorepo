name: CI Gates - KájovoHotel

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:

  e2e-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile=false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install API Python dependencies (minimal)
        run: |
          python -m pip install --upgrade pip
          python -m pip install "fastapi==0.128.1" "pydantic==2.9.2" "pydantic-settings==2.6.1" "sqlalchemy==2.0.36" "uvicorn[standard]==0.32.0"

      - name: Install Playwright browser for smoke
        run: pnpm --filter @kajovo/kajovo-hotel-admin exec playwright install --with-deps chromium

      - name: E2E smoke (3 deterministic runs)
        env:
          API_BASE_URL: http://127.0.0.1:18000
          SMOKE_DB_PATH: /tmp/kajovo-smoke-e2e.db
        run: |
          for i in 1 2 3; do
            echo "Smoke run #${i}"
            rm -f "$SMOKE_DB_PATH"
            pnpm ci:e2e-smoke
          done
  guardrails:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile=false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install API Python dependencies (minimal)
        run: |
          python -m pip install --upgrade pip
          python -m pip install "fastapi==0.128.1" "pydantic==2.9.2" "pydantic-settings==2.6.1" "sqlalchemy==2.0.36" "alembic==1.13.3" "uvicorn[standard]==0.32.0"

      - name: Parity placeholder gate
        run: pnpm ci:parity-placeholders

      - name: Verification doc gate
        run: pnpm ci:verification-doc

      - name: Policy sentinel gate
        run: pnpm ci:policy

      - name: Token-only lint
        run: pnpm ci:tokens

      - name: Brand assets lint (brand/** whitelist)
        run: pnpm ci:brand-assets

      - name: Install Playwright browser
        run: pnpm --filter @kajovo/kajovo-hotel-web exec playwright install --with-deps chromium

      - name: SIGNACE gate
        run: pnpm ci:signage

      - name: View-states gate
        run: pnpm ci:view-states

      - name: WCAG gate
        run: pnpm ci:wcag

      - name: Verify API contract + generated client freshness
        run: pnpm contract:check

  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install API dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ./apps/kajovo-hotel-api[dev]

      - name: Ruff lint
        run: python -m ruff check apps/kajovo-hotel-api/app apps/kajovo-hotel-api/tests

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile=false

      - name: Typecheck
        run: pnpm typecheck

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile=false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install API dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ./apps/kajovo-hotel-api[dev]

      - name: Unit tests
        run: pnpm unit
