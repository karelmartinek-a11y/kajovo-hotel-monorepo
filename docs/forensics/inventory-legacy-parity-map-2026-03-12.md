# Inventory Legacy -> Current Semantic Mapping (2026-03-12)

Source snapshot SHA: `44e4cde30b3f720ec0a46b15abc3b44ed1af061b`

## Verdict
Current inventory model in `apps/kajovo-hotel-api` is semantically complete against legacy ingredient/card split when mapped as:
- legacy `ingredient` => current `inventory_items`
- legacy `card` (IN/OUT/adjustment documents) => current `inventory_movements`
- legacy operational notes/history => current `inventory_audit_logs`

No semantic loss was found in required operational use-cases (stock state, movement history, document linkage, stocktake export, pictogram media).

## Use-case mapping

| Legacy use-case | Current implementation | Evidence |
|---|---|---|
| Ingredient CRUD | `POST/GET/PUT/DELETE /api/v1/inventory*` | `apps/kajovo-hotel-api/app/api/routes/inventory.py` |
| Card IN (receipt) | `POST /api/v1/inventory/{id}/movements` with `movement_type=in`, mandatory `document_reference` | Inventory route validation + tests |
| Card OUT (issue) | same endpoint with `movement_type=out` | Inventory route validation + tests |
| Adjustment / write-off | same endpoint with `movement_type=adjust` | Inventory route validation + tests |
| Document numbering | Generated by `_next_document_number` (`PR-YYYY-####`, `VY-YYYY-####`, `OD-YYYY-####`) | Inventory route `_next_document_number` |
| Movement history reconstruction | `GET /api/v1/inventory/{id}` includes ordered `movements` | `InventoryItemWithAuditRead` response |
| Pictogram/media | `POST /api/v1/inventory/{id}/pictogram` + `GET /pictogram/{kind}` | Inventory media handlers |
| Auditability | `_log_audit` writes to `inventory_audit_logs` on CRUD/movement events | Inventory route + DB model |
| Stocktake document | `GET /api/v1/inventory/stocktake/pdf` | `app/services/pdf/inventory.py` |

## Accounting/operational traceability
- Each movement carries `document_number`, `document_date`, `document_reference`, `quantity`, `movement_type`.
- Stock values are updated transactionally with movement writes/deletes.
- Audit log captures entity/action/detail and timestamp for post-fact forensic trace.

## Closure condition
Parity matrix row `Inventory, Ingredient/card split model + pictograms` is updated from `PARTIAL` to `FULLY_WORKING` with this mapping as evidence.
