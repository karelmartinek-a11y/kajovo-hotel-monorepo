# Staging hostname
STAGING_HOSTNAME=kajovohotel-staging.hcasc.cz
WEB_BASE_URL=https://kajovohotel-staging.hcasc.cz
API_BASE_URL=https://kajovohotel-staging.hcasc.cz

# Web exposure (host port mapped to web container)
WEB_PORT=8081

# Postgres (used when running local DB container)
POSTGRES_DB=kajovo_hotel_staging
POSTGRES_USER=kajovo
POSTGRES_PASSWORD=replace_with_strong_password

# API app metadata
KAJOVO_API_APP_NAME=KájovoHotel API
KAJOVO_API_APP_VERSION=0.1.0
KAJOVO_API_ENVIRONMENT=staging

# API DB connection string
# Local compose DB:
# KAJOVO_API_DATABASE_URL=postgresql+psycopg://kajovo:replace_with_strong_password@postgres:5432/kajovo_hotel_staging
# External managed DB example:
# KAJOVO_API_DATABASE_URL=postgresql+psycopg://<user>:<password>@<host>:5432/<db_name>
KAJOVO_API_DATABASE_URL=postgresql+psycopg://kajovo:replace_with_strong_password@postgres:5432/kajovo_hotel_staging
