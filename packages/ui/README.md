# @kajovo/ui

Design system for KájovoHotel. Styling MUST be token-driven via:
- `apps/kajovo-hotel/ui-tokens/tokens.json`
- `apps/kajovo-hotel/palette/palette.json`
- `apps/kajovo-hotel/ui-motion/motion.json`

Mandatory UI brand:
- `KajovoSign` must be present on every view outside PopUp and visible on scroll.
- Brand elements per view: min 1, max 2 (see Manifest.md).

This package is intentionally minimal at this stage; Codex refactor will extend it.
